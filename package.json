{
  "name": "harvesthub-root",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm install && npm run build",
    "install": "cd frontend && npm install"
  },
  "dependencies": {
    "@google/genai": "^2.19.0"
  },
  "devDependencies": {
    "@vercel/node": "^7.0.0"
  }
}
